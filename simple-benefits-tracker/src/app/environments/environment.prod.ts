export const environment = {
  production: true,
  apiUrl: 'https://render-api-url.onrender.com/api' // was hoping to get it on render but unclear if it can accommodate the version for visual basics backend - possibly too low
};
